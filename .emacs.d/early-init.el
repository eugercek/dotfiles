;;; early-init.el -*- lexical-binding: t; -*-

;; Allow pixel-precise frame resizing (prevents overshooting screen bounds)
(setq frame-resize-pixelwise t)

;; Maximized frame is set in init.el via after-init-hook

;;; early-init.el ends here
